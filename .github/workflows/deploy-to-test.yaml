# =============================================================================
# Deploy to Test
# =============================================================================
# Manual workflow — triggered via workflow_dispatch with an image version tag.
# No Docker build — uses upstream ghcr.io/docling-project/docling-serve-cpu.
# =============================================================================

name: Deploy to Test

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'docling-serve image tag to deploy (e.g. v1.21.0)'
        required: true
        default: 'v1.21.0'

concurrency:
  group: deploy-test
  cancel-in-progress: false

permissions:
  contents: read

env:
  OPENSHIFT_NAMESPACE_TEST: 6cdc9e-test
  HELM_RELEASE: eagle-demi

jobs:
  deploy:
    name: Deploy to Test
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Install OpenShift CLI
        run: |
          curl -LO "https://mirror.openshift.com/pub/openshift-v4/clients/ocp/latest/openshift-client-linux.tar.gz"
          tar -xvzf openshift-client-linux.tar.gz
          sudo mv oc /usr/local/bin/
          rm -f openshift-client-linux.tar.gz

      - name: Install Helm
        run: |
          curl -fsSL https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash
          helm version

      - name: Log into OpenShift
        run: oc login --token=${{ secrets.OPENSHIFT_TOKEN }} --server=${{ secrets.OPENSHIFT_URL }}

      - name: Deploy with Helm
        run: |
          helm upgrade --install ${{ env.HELM_RELEASE }} ./helm \
            --namespace ${{ env.OPENSHIFT_NAMESPACE_TEST }} \
            --values ./helm/values-test.yaml \
            --set image.tag=${{ github.event.inputs.version }} \
            --wait --timeout=10m

      - name: Verify deployment
        run: |
          echo "Verifying eagle-demi deployment..."
          oc rollout status deployment/${{ env.HELM_RELEASE }} \
            -n ${{ env.OPENSHIFT_NAMESPACE_TEST }} --timeout=10m
          echo "✓ eagle-demi healthy"

          echo ""
          echo "Pods:"
          oc get pods -n ${{ env.OPENSHIFT_NAMESPACE_TEST }} \
            -l app.kubernetes.io/instance=${{ env.HELM_RELEASE }}
