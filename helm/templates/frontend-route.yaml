{{- if and .Values.frontend.enabled .Values.frontend.route.enabled }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "eagle-demi.fullname" . }}-frontend
  labels:
    {{- include "eagle-demi.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  {{- if .Values.frontend.route.host }}
  host: {{ .Values.frontend.route.host }}
  {{- end }}
  {{- if .Values.frontend.route.path }}
  path: {{ .Values.frontend.route.path }}
  {{- end }}
  port:
    targetPort: http
  tls:
    termination: {{ .Values.frontend.route.tls.termination | default "edge" }}
    insecureEdgeTerminationPolicy: {{ .Values.frontend.route.tls.insecureEdgeTerminationPolicy | default "Redirect" }}
  to:
    kind: Service
    name: {{ include "eagle-demi.fullname" . }}-frontend
    weight: 100
  wildcardPolicy: None
{{- end }}
