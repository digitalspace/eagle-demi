{
  "version": "2.0",
  "_comment": "Host settings for the demi-secret-sync-<env> app only. The API's host.json at the repo root is a different app and neither file is packaged with the other. No queue section here: this app has no queue trigger.",
  "logging": {
    "logLevel": {
      "default": "Warning",
      "Host.Results": "Information",
      "Function": "Information"
    },
    "applicationInsights": {
      "enablePerformanceCountersCollection": false,
      "samplingSettings": {
        "isEnabled": true
      }
    }
  },
  "extensionBundle": {
    "id": "Microsoft.Azure.Functions.ExtensionBundle",
    "version": "[4.0.0, 5.0.0)"
  }
}
